# URLShortKaro

A URL shortening service built with:

- Java 21
- Spring Boot
- Spring Data JPA
- PostgreSQL
- Flyway

## Run locally

```bash
docker compose up -d
cd backend
./mvnw spring-boot:run
```